<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Exclude all .cs files from main project -->
    <Compile Remove="*.cs" />
    <!-- Include only the mock server program -->
    <Compile Include="MockSpoServerProgram.cs" />
  </ItemGroup>

</Project>
